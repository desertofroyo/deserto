Circular avatar with initials fallback.

```jsx
<Avatar name="Juan Smith" />
<Avatar src="/me.jpg" name="Jane" size={56} tone="orange" />
```

Without `src`, shows up-to-two initials on a warm tint. `tone`: wine | orange | olive | coffee.
