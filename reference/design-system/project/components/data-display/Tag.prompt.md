Selectable filter / topping chip.

```jsx
<Tag selected onClick={...}>Berries</Tag>
<Tag dotColor="var(--orange-500)" onClick={...}>Mango</Tag>
<Tag removable onRemove={...}>Granola</Tag>
```

Selected = wine fill. Add `dotColor` for a flavor swatch, `removable` for a × button.
